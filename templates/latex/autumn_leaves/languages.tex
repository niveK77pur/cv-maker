<<& let circle_size = 1.0 -&>>
<<& let inter_space_coeff = 0.7 -&>>
\begin{cvtblr}{
    colspec = *{<<< columns * 2 >>>}{Q[l,m]},
    column{even}={rightsep=<<< colsep >>>cm},
  }
  <<& for entry in languages -&>>

  <<< entry.language >>> & \raisebox{0.5\height}{%
    \tikz[baseline=(current bounding box.center)]{
      \foreach \n in {1,...,<<< entry.max_level_int() >>>}
      \draw[secondary] (<<< inter_space_coeff >>>*\n,0) circle (<<< circle_size >>>ex);
      \foreach \n in {1,...,<<< entry.current_level_int() >>>}
      \filldraw[secondary] (<<< inter_space_coeff >>>*\n,0) circle (<<< circle_size >>>ex);
    }%
  }
  <<& if loop.index % columns == 0 -&>>
  \\
  <<& else -&>>
  &
  <<& endif -&>>

  <<&- endfor -&>>
\end{cvtblr}
<<#
vim: tw=0 cole=0
#>>
